# Quick actions shown in the drawer's "Actions" tab, as a grid (2 per row).
# One action per line: Label|shell command
Copy|wtype -M ctrl c -m ctrl
Cut|wtype -M ctrl x -m ctrl
Paste|wtype -M ctrl v -m ctrl
